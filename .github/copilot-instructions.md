# Copilot Instructions for Circle Cycle

- Keep all game logic in `game/` modules.
- Keep the `ui/` modules focused on rendering and event forwarding.
- Load all characters, abilities, and cards from `data/*.json`.
- Use type hints on every public method.
- Add docstrings to every class and public method.
- Prefer small, composable objects over large shared state.
